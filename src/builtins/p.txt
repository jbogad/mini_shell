
IMPORTANTE:
    -Comandos que no necesitan execve(comandos internos)
        · echo y echo -n 
        · cd
        · pwd (con getwcd)
        · export (añade añade o modifica variables de entorno) 
        · unset (elimina variables de entorno)
        · env (muestra las variables de entorno)
        · exit (sale de la shell)

